# Cursor AI Rules - Korean Commit Messages

## 🚨 CRITICAL: Always Generate Korean Commit Messages

- Generate Commit Message 버튼을 클릭할 때 반드시 한국어로 커밋 메시지를 생성하세요.
- 커밋 메시지 생성 시 영어가 아닌 한국어를 우선적으로 사용합니다.
- 모든 커밋 메시지는 한국어로 작성되어야 합니다.

## 📝 Commit Message Rules (Korean)

- feat: 새로운 기능 추가
- fix: 버그 수정
- docs: 문서 업데이트
- refactor: 코드 리팩토링
- style: 코드 포맷팅
- test: 테스트 추가/수정

## 🛠️ 기술 스택 규칙

### UI 라이브러리
- shadcn/ui 컴포넌트만 사용 (필수)
- Material-UI, Ant Design 등 다른 UI 라이브러리 금지

### 스타일링
- Tailwind CSS 클래스 사용 (필수)
- 컴포넌트별 CSS 변수 활용 (권장)

### 상태 관리
- Zustand 사용 (권장)
- Context API (간단한 경우에만)
- Redux, MobX 금지

## 📁 프로젝트 구조

```
src/
├── components/
│   ├── ui/           # shadcn/ui 컴포넌트들 (수정 금지)
│   ├── [project]/    # 프로젝트별 컴포넌트
│   └── layout/       # 레이아웃 컴포넌트
├── pages/            # 프로젝트별 페이지
├── hooks/            # 커스텀 훅
├── store/            # Zustand 스토어
├── services/         # API 서비스
├── utils/            # 유틸리티 함수
└── constants/        # 상수
```

## 📝 코딩 컨벤션

### 파일명 규칙
- 컴포넌트: PascalCase.tsx
- 유틸리티: camelCase.ts
- 폴더: kebab-case/

### Import 순서
```typescript
// 1. React 및 외부 라이브러리
import React from 'react';

// 2. 내부 모듈
import { Button } from '@/components/ui/button';

// 3. 유틸리티 및 상수
import { formatDate } from '@/utils/date';
```

## 🌿 브랜치 네이밍
- [prefix]/[project-name]/[feature-description]
- feat/ecommerce/payment-system
- fix/dashboard/mobile-layout

## ⚠️ 금지사항
- main 브랜치 직접 커밋 금지
- UI 라이브러리 변경 금지
- 코어 아키텍처 수정 금지

**중요**: Cursor AI의 Generate Commit Message 기능 사용 시 반드시 한국어로 생성할 것!
